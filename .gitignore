# 파이썬 캐시
__pycache__/
*.py[cod]

# 구글 드라이브 동기화 임시 폴더
.tmp.drivedownload/
.tmp.driveupload/

# 로컬 도구 설정
.claude/

# 환경 변수 (API 키가 들어갈 수 있음)
.env
.env.*

# 구글 서비스 계정 키 (Firestore 접속용 비밀 파일 — 절대 커밋하지 않음)
service-account*.json

# OS
Thumbs.db
desktop.ini
.DS_Store

# 홍보 초안 (블로그 글·카드뉴스). 서비스 코드가 아니고, origin이 공개 저장소라 올리지 않는다.
홍보/
