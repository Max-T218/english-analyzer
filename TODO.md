# 다음에 할 일

작업하다 만 것, 하기로 정했지만 아직 안 만든 것을 적어 둔다.
끝나면 이 파일에서 지운다.

---

## 1. 문제 제작 — 200문항을 넘길 때 '나눠서 진행'

### 왜

`QUIZ_MAX_QUESTIONS_PER_RUN`(=200, `public/app.js`)을 넘기면 지금은 **그냥 거부**한다.

```
한 번에 200문항까지만 만들 수 있습니다
(지금 지문 40개 × 10문항 = 400문항). 지문·유형·문항 수 중 하나를 줄여 주세요.
```

그러면 선생님이 지문을 20개로 줄여 한 번 돌리고, 나머지 20개를 **다시 입력하고 유형도
다시 골라** 또 돌려야 한다. 설정을 두 번 하는 것이 번거롭다.

"많이 만들면 오류가 날 수 있습니다, 진행할까요?" 같은 경고창은 넣지 않기로 했다 —
막연한 위험 문구는 사람이 그냥 넘긴다. 이미 다 설정한 뒤라 되돌리기 아깝고, 선택지가
'멈춤/강행' 둘뿐이면 무조건 강행한다. 대신 **구체적인 숫자(시간·요금)를 보여 주고
중간 선택지를 준다.**

덧붙여, 그 경고 문구는 위험을 실제보다 크게 들리게 한다. 문항 6개마다 따로 호출하고
(`QUIZ_QUESTIONS_PER_CALL`), 일부가 실패해도 성공한 것은 남으며, **실패한 몫은 요금이
나가지 않는다**(`charge_krw`가 예외 처리들을 모두 지난 뒤에야 불린다 — server.py의
`/api/quiz` 처리부). 즉 실제 위험은 '오류'가 아니라 '오래 걸림'이다.

### 만들 것

200문항을 넘겼을 때 거부하는 대신, 이런 창을 띄운다.

```
400문항을 만듭니다
예상 시간 약 25분 · 예상 요금 약 40,000원
브라우저를 닫으면 중단됩니다.

[20개씩 나눠서 진행]  [그대로 진행]  [취소]
```

- **20개씩 나눠서 진행** — 지문을 200문항 이하로 들어가는 만큼씩 잘라 차례로 돌린다.
  한 묶음이 끝나면 결과를 화면에 붙이고 다음 묶음으로 넘어간다.
- **그대로 진행** — 지금의 200 제한을 무시하고 한 번에 간다.
- **취소** — 아무것도 하지 않는다.

묶음마다 다시 묻지는 않는다. 처음에 한 번만 묻고 끝까지 간다 — 지문이 100개면 5번을
물어야 해서 오히려 성가시다. 대신 진행 표시(`loadingTextEl`)에 "2/5 묶음"처럼 어디까지
왔는지 보여 준다.

### 손댈 자리

줄 번호는 밀리니 심볼 이름으로 찾을 것.

| 무엇 | 어디 |
|---|---|
| 200 제한과 거부 메시지 | `public/app.js`의 `QUIZ_MAX_QUESTIONS_PER_RUN`, `setupQuizTab` 안 `generate()`에서 `runQuestions > QUIZ_MAX_QUESTIONS_PER_RUN` 검사하는 곳 |
| 지금의 요금 확인창 | `costConfirmed(krw, label, jobCount)` — **브라우저 기본 `confirm()`이라 버튼이 두 개뿐이다.** 3지 선택을 하려면 별도 창이 필요하다 |
| 따라 만들 창 | `#printGuide`(index.html) + `showPrintGuide()`(app.js) — 이 앱의 커스텀 모달 본보기. `.modal-overlay` 계열도 있다(`#assignModalOverlay`) |
| 지문 목록 | `generate()` 안 `const jobs = passageMgr.getJobs();` — 이 배열을 잘라 묶음을 만든다 |
| 한 호출당 문항 수 | `QUIZ_QUESTIONS_PER_CALL`(=6). 총 호출 수 ≈ 지문 수 × 변형 세트 수 × `chunkTypes()`가 만든 묶음 수 |
| 진행 문구 | `loadingTextEl.textContent` |

### 주의할 점

- **'전체 문항 섞기'와 겹친다.** 그 모드는 모든 지문이 끝나야 섞을 수 있어서 `randomBuckets`에
  모아 두었다가 마지막에 한 번에 그린다. 묶음으로 나눠 돌려도 **마지막 묶음이 끝난 뒤에
  전체를 한 번에 섞어야** 한다 — 묶음마다 따로 섞으면 묶음 경계를 넘어 섞이지 않는다.
- **중간에 멈춰도 그때까지 만든 것은 남겨야 한다.** 지금도 그렇게 설계돼 있다
  (`append()`로 완성될 때마다 붙이고, `showFooter()`로 인쇄·저장 버튼을 연다).
- **요금은 총량 그대로다.** 나눠 돌린다고 싸지지 않는다. 다만 중간에 그만두면 그만큼만 나간다.
- **예상 시간은 실측값이 필요하다.** 호출 1회에 몇 초 걸리는지 재서 상수로 두고
  `호출 수 × 초`로 계산할 것. 근거 없는 숫자를 보여 주면 안 된다.
- 서버는 손댈 필요가 없다. 200 제한은 화면에만 있고, 서버는 요청 하나(지문 1개)씩 받는다.

### 확인 방법

1. 지문 40개 + OX진위(영/한) 각 5문항 = 400문항으로 '문제 만들기' → 창이 뜨는지
2. **[20개씩 나눠서 진행]** → 20개씩 두 묶음으로 도는지, 진행 문구가 "1/2 → 2/2"로 바뀌는지
3. 1묶음이 끝난 시점에 이미 만들어진 문항이 화면에 있는지(중간에 창을 닫아도 남는지)
4. '전체 문항 섞기'를 켜고 같은 걸 돌려 → **모든 묶음이 끝난 뒤** 400문항이 한 벌로 섞이는지
5. **[그대로 진행]** → 400문항을 한 번에 도는지
6. 200문항 이하일 때는 창이 안 뜨고 지금까지대로 도는지
