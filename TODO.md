# 다음에 할 일

작업하다 만 것, 하기로 정했지만 아직 안 만든 것을 적어 둔다.
끝나면 이 파일에서 지운다.

---

## 지문 순서를 바꿀 수 있게 한다

2026-09-12에 **하기로 정했다.** 설계는 아직 안 정했다 — 아래는 손대기 전에 정해야
할 것과, 코드를 읽어 보고 미리 걸리는 자리를 적어 둔 것이다.

### 왜 필요한가

지금 지문칸에서 할 수 있는 것은 `＋ 지문 추가` · `✕ 삭제` · `↑↓ 합치기` · `✂ 나누기`
뿐이고 **자리를 옮기는 길이 없다.** 순서가 곧 인쇄물의 순서인데, PDF에서 한 번에
25개를 꺼내거나(`runPdfImport`) 저장본을 뒤에 이어 붙이면(`appendJobs`) 원하는 차례로
들어오지 않는다. 지금은 지문을 잘라내어 다시 붙여넣는 수밖에 없다.

### 정해야 할 것

| 항목 | 후보 |
|---|---|
| 옮기는 방법 | ① `↑ ↓` 버튼 ② 끌어 옮기기 ③ 둘 다 |
| 되돌리기 | 합치기·나누기처럼 `item._opUndo`에 직전 목록을 통째로 담을지 |

**`↑ ↓` 버튼만으로는 부족할 수 있다** — 지문칸 상한이 `MAX_PASSAGES`(40)이라
맨 아래 것을 맨 위로 올리려면 39번을 눌러야 한다. 반대로 **끌어 옮기기만 두면** 긴
목록에서 스크롤과 싸우게 되고, 지문칸은 칸 하나가 화면을 거의 채울 만큼 크다
(`🔍 크게 보기`로 더 커진다). 둘 다 두는 쪽이 무난해 보이지만, 머리줄 버튼이 이미
다섯 개라 더 붐빈다는 점은 같이 봐야 한다.

### 미리 걸리는 자리 (코드를 읽고 확인한 것)

- **이미 만들어 둔 결과물과 어긋난다.** 분석본·문제는 만들 때의 지문 차례로 그려져
  있어서, 결과물을 띄워 둔 채 지문 순서만 바꾸면 화면의 1번 지문과 결과물의 1번이
  서로 다른 것을 가리킨다. `loadSavedItem`이 지문을 갈아 끼울 때 `clearAllTabResults()`로
  결과물을 지우는 것과 **똑같은 문제**다. 그때처럼 지울지, 아니면 "결과물과 순서가
  어긋났습니다"라고 알리기만 할지 정해야 한다. 지문 하나를 옮겼다고 만들어 둔 것을
  전부 날리면 그게 더 큰 손해일 수 있다.
- **직접 지은 이름은 따라오지 않는다.** 이름을 비워 두면(`named:false`) `renumber()`가
  placeholder를 "지문 1·2·3"으로 다시 매겨 저절로 맞는다. 그런데 선생님이 손으로
  "지문 3"이라고 **적어 둔** 경우(`named:true`)는 자리를 옮겨도 이름이 "지문 3"으로
  남아, 1번 자리에 "지문 3"이 앉는다. 옮길 때 알릴지 말지 정해야 한다.
- **시험지 탭은 지문칸이 따로다.** `EXAM_PAPER_MAX_PASSAGES`를 쓰는 쪽에도 같은 기능이
  필요한지 함께 볼 것 — `createPassageManager`를 공유하면 한 번에 되고, 아니면 두 번 만든다.

### 손대야 하는 자리

전부 `public/app.js`의 `createPassageManager` 안이다. 서버는 건드릴 것이 없다.

- `addRow` — 머리줄에 옮기기 버튼 추가(지금 `.passage-merge-up`/`.passage-merge-down`이
  붙어 있는 자리)
- `renumber` — 자리를 옮긴 뒤 번호·placeholder·버튼 숨김 상태를 다시 맞춘다. 이미
  하는 일이라 부르기만 하면 된다
- `mergeRows` / `splitRow` — 목록 전체가 바뀌는 같은 성격의 작업이다. 되돌리기를
  붙인다면 이 둘이 쓰는 `_opUndo` 방식을 그대로 따라갈 것
- `CHANGELOG`(`server.py`) — 화면에 드러나는 기능이라 항목을 하나 넣는다

---

## 학생 로그인에 아이디·비밀번호를 붙인다 (설계만 끝, 보류)

2026-09-10에 **설계까지 정하고 만들지는 않기로 했다.** 학생 기능 자체가 보류
상태여서다(아래 개인정보처리방침 항목 참고).

### 왜 필요한가

지금 `login_student`는 **이름만 받는다.** 비밀값이 전혀 없어서 이름을 아는 사람이면
누구나 그 학생으로 들어간다. 단어시험까지는 넘어갔지만, **앞으로 하려는 것(선생님이
낸 객관식·주관식을 학생이 풀고 오답을 확인하는 구독형)** 에서는 성적이 남으므로
그대로 쓸 수 없다. "내가 낸 답이 아니다"를 가릴 방법이 없다.

덤으로 지금의 **이름 전체 유일 제약도 풀린다.** 이름이 조회 키라서 앱 전체에서
유일해야 했고(`create_student`가 등록 시점에 거부한다), 흔한 이름일수록 다른
선생님 반과 부딪혔다. 아이디가 키가 되면 이 제약이 사라진다.

### 정한 것

| 항목 | 결정 |
|---|---|
| 아이디 유일 범위 | **앱 전체** |
| 아이디 짓는 주체 | **서버가 제안**(`민수` → `minsu`, 겹치면 `minsu2`), 선생님은 확인·수정만 |
| 초기 비밀번호 | `123456` 고정 |
| 첫 로그인 | 비밀번호를 **바꿔야만** 안으로 들어갈 수 있다 |
| 분실 대응 | 담당 선생님이 초기화 → `123456`으로 되돌리고 변경 강제도 다시 켠다 |
| 로그인 차단 | **아이디별** 5회/5분 (지금은 IP별) |

**아이디를 앱 전체 유일로 정한 이유**는 학원에서 한 학생이 여러 선생님 수업을 듣는
경우다. 반 단위로 나누면 그 학생에게 반마다 계정이 하나씩 생겨, 나중에 "이 학생이
지금까지 푼 문제와 오답"을 모으기 어렵고 구독 과금도 한 사람에게 여러 번 걸린다.
앱 전체 유일이면 **학생 한 명 = 계정 하나**로 떨어진다. 반 코드를 로그인에서 다시
묻지 않아도 되는 것은 덤이다.

**아이디를 선생님이 직접 짓게 하지 않는 이유**는 둘이다. ① 앱 전체 유일이면 흔한
아이디를 먼저 만든 선생님이 선점해, 지금 이름에서 겪는 충돌이 아이디로 자리만
옮긴다. ② 30명치 아이디를 머리로 짜내는 부담이 생긴다 — 지금은 이름만 치면 끝인데
입력이 두 배가 된다. 서버가 붙여 주면 선생님 부담이 지금과 같다.

### 반드시 함께 해야 하는 것

- **초기 비밀번호를 안 바꾼 학생은 아무것도 못 하게 막는다.** 학생 문서에 '아직 초기
  비밀번호' 표시를 두고, 켜져 있으면 비밀번호 변경 화면 말고는 시험 목록조차 보이지
  않게 한다. **선택으로 두면 아무도 안 바꿔서, 반 전체가 `123456`인 채로 남는다** —
  비밀번호가 없던 때와 같은데 있다고 착각하게 되는 만큼 더 나쁘다.
- **새 비밀번호로 `123456`을 다시 못 쓰게 막는다.** 안 막으면 변경 화면에서 그대로
  넣고 넘어간다.
- **초기화는 '처음 등록 상태로 되돌리기'여야 한다** — 비밀번호만 되돌리고 변경 강제
  표시를 안 켜면 그 학생만 `123456`으로 방치된다.
- **비밀번호 규칙은 느슨하게.** 대문자·특수문자 요구는 넣지 않는다. 중고생이 잊어버리면
  그 부담이 전부 선생님 초기화 요청으로 돌아온다. 4자 이상에 `123456` 금지 정도.
- **초기화는 담당 선생님이 자기 반 학생에게만.** `students` 문서의 `teacher_id`를
  확인하는 기존 방식(`delete_student`·`list_students`)을 그대로 따른다.
- **초기화한 시각을 학생 문서에 남긴다.** 초기화 순간부터 선생님도 그 학생 비밀번호를
  아는 상태가 되므로, "내가 낸 답이 아니다" 분쟁이 생겼을 때 판단 근거가 된다.
  값 하나 적는 것이라 부담이 없다.

### 손대야 하는 자리

- `create_student` — 아이디 생성·중복 회피, 초기 비밀번호 해시(`_hash_password` 재사용),
  '초기 비밀번호' 표시. 이름 전체 유일 검사는 걷어낸다
- `login_student` — 이름 대신 아이디+비밀번호를 받는다
- `_student_login_blocked` / `_student_login_fails` — 기준을 IP에서 아이디로.
  지금은 한 교실이 같은 IP로 보여, 성공 없이 실패만 5번 쌓이면 반 전체가 잠긴다
  (성공하면 그 IP 기록이 통째로 지워져 실제로는 덜 터지지만, 비밀번호가 생기면
  오타가 늘어 자주 걸릴 것이다)
- 새 API — 학생용 비밀번호 변경, 선생님용 초기화
- `public/student.js` — 첫 로그인 시 비밀번호 변경 화면
- `public/app.js`의 학생 관리 탭 — 초기화 버튼, 아이디·초기 비밀번호 인쇄용 쪽지
- 개인정보처리방침 — 수집 항목에 아이디·비밀번호가 늘어난다 (아래 항목과 함께)

---

## 학생 데이터가 개인정보처리방침에 없다

`students` / `test_assignments` / `test_attempts` 컬렉션에 **학생 이름·답안·점수**가
쌓이는데, [개인정보처리방침](public/privacy.html)에 '학생'이라는 말이 한 번도 나오지
않는다. 수집 항목·처리 목적·보유 기간 어디에도 없고, **만 14세 미만 법정대리인
동의**를 누가 받는지도 정해져 있지 않다.

2026-08-31에 **미루기로 했다** — 반/학생 기능은 `classroom_approved`가 켜진 계정만
쓸 수 있고, 지금은 운영자 본인만 켜져 있어 남의 학생 데이터가 쌓일 일이 없다.

⚠️ **다른 선생님에게 `classroom_approved`를 켜 주는 순간 얘기가 달라진다.**
`admin.html`에서 그 버튼을 누르기 전에 아래를 먼저 채울 것.

1. **처리방침 1항(수집 항목)** — 학생 이름(교사가 입력), 학생 세션 쿠키,
   시험 답안·점수·응시 회차
2. **처리방침 2항(처리 목적)** — 단어시험 배정·채점·결과 확인
3. **처리방침 3항(보유 기간)** — 학생 삭제 / 반 삭제 / 교사 탈퇴 시 파기
4. **신설 항목** — 만 14세 미만은 법정대리인 동의가 필요하며, **그 동의를 받을
   책임은 학생을 등록하는 교사에게 있다**는 것. 지금은 누가 받는지 아무 데도 없다.
5. **이용약관** — 3조 정의에 "학생 이용자"를 넣는다. 지금 약관의 "이용자"는 회원만
   가리키는데, 학생은 회원이 아니면서 서비스를 쓴다.
6. **학생 로그인에 비밀값이 없다는 것**을 약관에 적는다 — 이름만 알면 누구나 그
   학생으로 로그인된다(`login_student`). "시험 결과가 뒤바뀌었다"는 항의가 왔을 때
   미리 알렸는지가 갈림길이 된다.

---

## 약관·정책에 아직 안 넣은 것

2026-08-31에 공정거래위원회 **전자상거래 표준약관(제10023호)** 및 외부 검토 의견과
한 조씩 대조했다. 큰 것들은 그날 채웠고(폐업 시 포인트 보상·미성년자 결제 고지·면책
조항·환급 기한·회사의 의무·통지·자격 상실 단계·피해보상), **2026-09-01에 나머지를
채웠다**(시행일 2026-09-01, `TERMS_VERSION` 함께 올림).

- ~~ID·비밀번호 관리 의무~~ → 12조에 3개 항으로. 학생 로그인에 비밀값이 없다는 고지도 같은 자리
- ~~가입 시 청약철회·환불조건을 따로 확인받기~~ → 충전 창에 '결제 전에 꼭 확인해 주세요' 상자
- ~~청약철회 제한 사유를 미리 명기~~ → 8조 6항. **미리 알리지 않으면 그 제한을 주장하지 않는다**고 못박음
- ~~휴면계정~~ → 14조 6항(1년 미접속, 30일 전 통지, 잔액 유지)
- ~~개정 약관은 그 뒤에 맺는 계약에만 적용~~ → 4조 4항
- ~~결제수단별 수수료를 따로 받지 않는다~~ → 7조 6항
- ~~충전 확인 통지~~ → 7조 7항

### 남은 것

- **가입 화면**에도 청약철회·환불 조건을 따로 보여 줄지 — 표준약관 3조②는 '계약 체결
  전'을 말하므로 결제 창(지금 넣은 자리)이면 충분하다고 봤다. 가입만으로는 돈이 오가지
  않는다.

## 요약 그림 모델 — 급하지 않다 (원가는 이미 값으로 덮었다)

**먼저: 이건 이제 급한 일이 아니다.** 이 글은 요약 이미지가 250P이던 때 쓴 것이라
원가를 낮추는 게 절박하게 적혀 있었는데, 2026-08-31에 500P로 올리면서 사정이 달라졌다.

| 판매가 | 원가율 | 장당 남는 돈 |
|---|---|---|
| 250P (옛날) | 75.2% | 62원 |
| **500P (지금)** | **37.6%** | **312원** |

2026년 8월 청구서(158,000원)와 원장을 대조해 확인한 값이다. 텍스트 기능의 원가율이
27.2%이니 이미지가 조금 높긴 하지만, **장당 남는 돈은 둘이 거의 같다**(312원 대 329원).
게다가 이미지는 선택 기능이라 안 쓰면 그만이다.

원가를 94원으로 낮춰 봐야 장당 94원을 더 버는 것이고, 그 대가로 여섯 장 모두 실패했던
모델로 되돌아가는 위험을 진다. 지금 그 거래는 남는 장사가 아니다.

**그래도 언젠가 다시 볼 때를 위해** 남겨 둔다. 경위와 시험 방법은 `server.py`의
`MODEL_IMAGE` 주석에 있다. 요지만 옮기면:

- 2026-08-31에 나노바나나 2 · 1K(94원)로 옮겼다가 여섯 장 모두 사고가 나서 되돌렸다.
- **프로 · 1K** — 프로는 1K와 2K가 같은 값이라 원가가 안 내려간다. 값을 아끼는 길은
  아니고, "1K 자체가 문제였나"를 가리는 시험으로만 뜻이 있다.
- **나노바나나 2 · 2K**(141원) — 표본이 둘뿐이라 아직 모른다.
- 어느 쪽이든 **여러 장 뽑아 보고** 정할 것. 두 장만 보고 옮겼다가 배포한 뒤에 드러났다.

다시 급해지는 때는 하나뿐이다 — **이미지 값을 다시 내리기로 할 때.**
