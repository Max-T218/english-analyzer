# 다음에 할 일

작업하다 만 것, 하기로 정했지만 아직 안 만든 것을 적어 둔다.
끝나면 이 파일에서 지운다.

---

## 포인트 소진기한 — 5년으로 정했고, 소멸 기능은 아직 없다

충전 창·[이용약관](public/terms.html) 7조 4항·[환불정책](public/refund.html) 3항 세 곳에
**"유상 포인트의 소진기한은 결제한 시점으로부터 5년"**이라고 적혀 있는데,
**소멸시키는 코드가 없다.**

- 카드 충전 원장 줄에 `expires_at`이 붙지 않는다 (`add_krw`/`confirm_payment_intent`
  둘 다 유상분에는 안 넣는다. `_free_expiry()`는 `kind == "grant"` 무상분 전용이고,
  그마저 `FREE_POINT_EXPIRE_DAYS = 0`이라 항상 `None`)
- `expires_at`을 읽어 잔액을 깎는 곳이 **어디에도 없다**. `list_usage`가 화면에
  보여 주기만 한다

**급하지 않다.** 지금 상태는 고지한 것보다 더 주는 방향이고, 첫 결제가 일어나기
전에는 대상 자체가 없다. 5년이라 여유도 길다.

### 경위 (2026-08-31)

원래 **1년**이라고 적혀 있었다. 외부 검토에서 *"유상 포인트를 1년 만에 100%
소멸시키는 것은 약관규제법·공정거래위원회 표준약관 기준상 소비자의 재산권을 과도하게
침해하는 불공정 약관으로 해석될 수 있다(상법상 소멸시효 5년)"*는 지적을 받아 5년으로
고쳤다. **1년으로 소멸 기능을 만들지 않았던 것이 결과적으로 다행이었다** — 만들었다면
불공정 약관을 실제로 집행한 셈이 됐다.

함께 넣은 문구: **소진기한 전에는 언제든 남은 잔액을 환불받을 수 있다.** 재산권
침해 시비를 막는 실질적인 방패는 소진기한 길이보다 이쪽이다.

### 나중에 만들 때

1. 충전분마다 산 날짜를 남긴다 — 지금은 `krw_paid` 총액 하나뿐이라 어느 충전분이
   기한을 넘겼는지 못 가린다.
2. 잔액을 읽을 때 만료분을 걷어낸다. 이 앱에는 정기 실행(배치·크론)이 없어 밤에
   훑는 방식은 못 쓴다.
3. 이용 내역에 소멸 예정일과 소멸 기록을 함께 보여 준다 — 고지 없이 지우면 안 된다
   (`FREE_POINT_EXPIRE_DAYS` 주석에 적어 둔 원칙).

### 아직 안 본 것

외부 검토에서 유상 포인트를 **"선불전자지급수단(캐시)"**이라고 불렀다. 정말 그에
해당하면 전자금융거래법상 등록 의무가 따라올 수 있다. 다만 그 법은 보통 '2개 이상
업종에서 쓰이는 것'을 대상으로 하고 이 포인트는 자기 서비스에서만 쓰이므로 해당하지
않을 가능성이 크다 — 확인은 받아 두는 게 좋다.

---

## 학생 데이터가 개인정보처리방침에 없다

`students` / `test_assignments` / `test_attempts` 컬렉션에 **학생 이름·답안·점수**가
쌓이는데, [개인정보처리방침](public/privacy.html)에 '학생'이라는 말이 한 번도 나오지
않는다. 수집 항목·처리 목적·보유 기간 어디에도 없고, **만 14세 미만 법정대리인
동의**를 누가 받는지도 정해져 있지 않다.

2026-08-31에 **미루기로 했다** — 반/학생 기능은 `classroom_approved`가 켜진 계정만
쓸 수 있고, 지금은 운영자 본인만 켜져 있어 남의 학생 데이터가 쌓일 일이 없다.

⚠️ **다른 선생님에게 `classroom_approved`를 켜 주는 순간 얘기가 달라진다.**
`admin.html`에서 그 버튼을 누르기 전에 아래를 먼저 채울 것.

1. **처리방침 1항(수집 항목)** — 학생 이름(교사가 입력), 학생 세션 쿠키,
   시험 답안·점수·응시 회차
2. **처리방침 2항(처리 목적)** — 단어시험 배정·채점·결과 확인
3. **처리방침 3항(보유 기간)** — 학생 삭제 / 반 삭제 / 교사 탈퇴 시 파기
4. **신설 항목** — 만 14세 미만은 법정대리인 동의가 필요하며, **그 동의를 받을
   책임은 학생을 등록하는 교사에게 있다**는 것. 지금은 누가 받는지 아무 데도 없다.
5. **이용약관** — 3조 정의에 "학생 이용자"를 넣는다. 지금 약관의 "이용자"는 회원만
   가리키는데, 학생은 회원이 아니면서 서비스를 쓴다.
6. **학생 로그인에 비밀값이 없다는 것**을 약관에 적는다 — 이름만 알면 누구나 그
   학생으로 로그인된다(`login_student`). "시험 결과가 뒤바뀌었다"는 항의가 왔을 때
   미리 알렸는지가 갈림길이 된다.
