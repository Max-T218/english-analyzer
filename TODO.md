# 다음에 할 일

작업하다 만 것, 하기로 정했지만 아직 안 만든 것을 적어 둔다.
끝나면 이 파일에서 지운다.

---

## 포인트 소진기한 — 적어 놓은 것과 실제가 다르다

충전 창·[이용약관](public/terms.html)·[환불정책](public/refund.html) 세 곳에
**"유상 포인트의 소진기한은 결제한 시점부터 1년, 미사용 포인트는 소멸"**이라고
적혀 있는데, **소멸시키는 코드가 없다.**

- 카드 충전 원장 줄에 `expires_at`이 붙지 않는다 (`add_krw`/`confirm_payment_intent`
  둘 다 유상분에는 안 넣는다. `_free_expiry()`는 `kind == "grant"` 무상분 전용이고,
  그마저 `FREE_POINT_EXPIRE_DAYS = 0`이라 항상 `None`)
- `expires_at`을 읽어 잔액을 깎는 곳이 **어디에도 없다**. `list_usage`가 화면에
  보여 주기만 한다

지금 상태는 **고지한 것보다 더 주는 방향**이라 급한 사고가 아니다. 반대로 고지 없이
소멸시키는 쪽이 분쟁이 된다. 그래서 2026-08-29에 **카드사 실질심사가 끝날 때까지
손대지 않기로** 정했다 — 결제 관련 코드는 심사 전에 건드리지 않기로 한 것과 같은
이유이고, 충전 창·약관에 이 문구가 있는 편이 심사에 오히려 유리하다.

**심사 통과 후 다시 볼 것:**

1. 기간을 몇 년으로 할지 먼저 정한다. 상거래 채권 소멸시효가 5년이라 1년 조항은
   나중에 다퉈질 여지가 있다 — 법률 확인이 필요한 대목이다.
2. 기간이 정해지면 둘 중 하나를 한다.
   - **코드를 문구에 맞춘다**: 충전분마다 산 날짜를 남기고(지금은 `krw_paid` 총액
     하나뿐이라 어느 충전분이 기한을 넘겼는지 못 가린다), 잔액을 읽을 때 만료분을
     걷어낸다. 이 앱에는 정기 실행(배치·크론)이 없어 밤에 훑는 방식은 못 쓴다.
     이용 내역에 소멸 예정일과 소멸 기록도 함께 보여 줘야 한다 — 고지 없이 지우면
     안 된다(`FREE_POINT_EXPIRE_DAYS` 주석에 이미 적어 둔 원칙).
   - **문구를 코드에 맞춘다**: 세 곳의 소진기한 문장을 지우거나 "소진기한 없음"으로
     바꾼다.
