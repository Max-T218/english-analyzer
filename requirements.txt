# 이 앱은 대부분 Python 표준 라이브러리만 사용합니다.
#
# ⚠️ 버전을 반드시 고정해 두세요(== 로). 예전에는 이름만 적어 두었는데, Render는 배포할
#    때마다 여기 적힌 것을 새로 설치하므로 '코드는 그대로인데 배포만 하면 갑자기 깨지는'
#    일이 생깁니다. 실제로 2026-08-25에 그렇게 사이트가 멈췄습니다 —
#    google-cloud-firestore 2.29.0이 그날 아침 나왔고, 관계없는 수정을 배포하자마자
#    그 새 버전이 딸려 들어와 Firestore 접근이 전부 400 Invalid database id (default)로
#    실패했습니다(로그인이 막히니 사이트 전체가 멈춤). 2.28.1로 되돌려 복구했습니다.
#
#    버전을 올릴 때는 배포 직후 로그인 한 번을 꼭 확인하세요.

# 저장/불러오기(구글 로그인 + Firestore) 기능을 위해 구글 공식 클라이언트만 추가합니다.
google-cloud-firestore==2.28.1
# PDF에서 지문 꺼내기(/api/pdfsplit)에 씁니다 — PDF 안의 글자층과 그 좌표를 읽습니다.
# 없어도 서버는 그대로 뜨고, 이 기능만 꺼집니다(server.py의 PDF_READY 참고).
pdfminer.six==20260107
